import React from "react";
import axios from "axios";
import { Button, message } from "antd";

const Home = () => (
  <>
    <div className="container">
      <h1>Welcome to SupaLana</h1>
    </div>
    {/* <Button onClick={logout}>Logout</Button> */}
  </>
);
export default Home;
